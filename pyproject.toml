[project]
name = "majsq-bot"
version = "0.1.0"
description = "The Telegram surface for maj$q — group-chat night planning."
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
